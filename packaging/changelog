moira (0.3.0) unstable; urgency=medium

  * Package 1: local History foundation and privacy-safe persistence — an
    on-device, race-free SQLite store under the user state directory with
    integrity checks; history never leaves the machine.
  * Package 2: History interface with selectable 24h/7d/30d/90d ranges and
    per-service filters, quota series with reset transitions, and
    local-time rendering.
  * Package 3: exact daily token totals and summaries from official Codex
    account usage, typed availability semantics and semantic schema v4
    invariants — Claude remains percentage-only with no estimates.
  * Package 4: exact statistics and activity indicators with
    capability-aware rendering — exact values are shown only where the
    provider contract supports them, never generic balances.
  * Package 5: complementary UX — per-provider collection toggles and
    typed alert rules, native desktop and NTFY notification channels with
    deduplicated exhaustion, recovery, reset and error events, sanitized
    diagnostics with copy actions, deterministic CSV/JSON export, history
    deletion, manual update checks with strict SemVer parsing, compact
    mode, and persisted settings (configuration schema v3).
  * Package 6: 0.3.0 release integration and artifact hygiene,
    privacy-minimal agent activity visibility through owned Claude Code
    and Hermes hooks and the documented Codex app-server protocol with
    hashed identities, multi-turn correction, and Debian policy compliance
    with reproducible packaging; publication remains deferred.
  * Package 7: read-only integration registry, Hermes inventory and local
    provider profiles — an Integrations page with an Agents section
    (controls moved from Settings without behavior change) and a
    Providers and models inventory with immutable provider-neutral types,
    exact states, sanitized Hermes main/named model assignments from the
    installed Hermes CLI, truthful capability badges (Claude
    percentage-only, unknown balance/cost never zero), bounded
    newest-wins probing with generations, and an Edit providers view
    managing local profiles (config v4) with per-profile Keyring
    credentials (local only, HTTPS for remote base URLs, blank input
    preserves, removal confirmed, recoverable writes through a durable
    journal with persist-before-forward ordering, exact journal and
    profile validation, mandatory backup cleanup, idempotent recovery
    at startup through a bounded and reaped subprocess and no
    credential loss, and other settings saves never wiping profiles);
    each profile row adds an explicit bounded read-only connection
    test (Keyring credential via a private child stdin pipe delivered
    inside the total deadline, documented Models endpoints only,
    classification before any Keyring read or spawn, a single validated
    DNS resolution carried as an immutable family/socktype/proto/
    sockaddr target with a direct connect and a getpeername check
    before any credential, nonblocking stdin delivery inside the
    deadline, strict Models JSON parsing, connect/read/total deadlines
    with reaping,
    ten sanitized states, one in-flight plus one newest pending, stale
    results discarded on edit/remove/disable/close, nothing persisted);
    each DeepSeek profile row also adds an explicit Refresh balance
    action against the official read-only /user/balance endpoint
    (Bearer auth, never the profile URL) with exact Decimal-parsed
    amounts rendered separately in deterministic currency order (Total,
    Granted and Topped up; CNY then USD), a strict payload contract
    (documented keys only, a real is_available boolean, a non-empty
    bounded array, unique CNY/USD entries, exact string amounts; extra,
    missing, secret/account-bearing or malformed data invalid), strict
    status mapping (200 to available/insufficient from is_available,
    401/403 auth failed, 402 insufficient without invented amounts, 429
    rate limited, 5xx server error, transport and TLS distinct,
    everything else invalid) through the same bounded child boundary
    (private stdin, single validated resolution, peer check before
    TLS/headers, verified TLS, no redirects or proxies, bounded
    time/body/output, reaping) with minimal canonical JSON on stdout
    only and ephemeral results that never imply token, cost or usage
    support, reusing the same linearizable coordinator;
    Hermes writes, financial units and publication remain deferred.

 -- Thibault Paul <xpaul.thibault@gmail.com>  Wed, 05 Aug 2026 11:00:00 +0200
