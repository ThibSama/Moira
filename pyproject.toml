[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "moira-quota-monitor"
version = "0.3.0"
description = "Ubuntu quota monitor for Claude and Codex"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]
authors = [{name = "Moira contributors"}]
dependencies = []

[project.scripts]
moira = "moira.app:main"
moira-claude-statusline = "moira.claude_integration:statusline_main"
moira-agent-hook = "moira.agent_hooks:agent_hook_main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.lint.per-file-ignores]
"src/moira/app.py" = ["E402"]
"src/moira/ui.py" = ["E402"]
"src/moira/secrets.py" = ["E402"]
"src/moira/history_chart.py" = ["E402"]
"src/moira/history_page.py" = ["E402"]
"src/moira/activity_view.py" = ["E402"]
"src/moira/integrations_page.py" = ["E402"]
"tests/test_package5_ui.py" = ["E402"]
"tests/test_activity_view.py" = ["E402"]

[tool.mypy]
python_version = "3.11"
strict = true
files = ["src/moira", "tests"]
exclude = ["src/moira/ui.py", "src/moira/app.py", "src/moira/secrets.py", "src/moira/history_chart.py", "src/moira/history_page.py", "src/moira/activity_view.py", "src/moira/integrations_page.py"]
